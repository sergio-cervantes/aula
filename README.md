# React + Vite

A simple demo of a MERN stack, each main layer is hold by a Kubernetes pod: frontend, backend and Mongo.

An Ingress Controller is required to let the access between the browser and the frontend, so the aula.local entry in hosts must exists in the client machine.

Access the demo site as:

http://aula.local
